import type { ReferenceInformation } from './reference-information-management.types'

// Placeholder mapper - to be implemented when API is available
export const mapReferenceInformation = (dto: unknown): ReferenceInformation => {
  // Placeholder implementation
  return dto as ReferenceInformation
}

