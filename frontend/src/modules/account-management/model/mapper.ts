import type { AccountManagement } from './account-management.types'

// Placeholder mapper - to be implemented when API is available
export const mapAccountManagement = (dto: unknown): AccountManagement => {
  // Placeholder implementation
  return dto as AccountManagement
}
